---
description: Inventaire figé FitMangas — surfaces app, acquis, anti-régression. Toujours appliquer.
alwaysApply: true
---

# FitMangas — Inventaire figé (ne pas casser / réécrire à l’aveugle)

> Complète `fitmangas-source-de-verite.mdc`.  
> **Avant toute modif** d’une surface listée : lire ce fichier + le dossier `docs/archive-produit/`.  
> Quand une étape est **validée en prod**, mettre à jour `docs/CONTEXTE_VIVANT.md` (section du jour) **dans le même commit**.

## Règle d’or anti-régression
1. Ne pas « refondre » une feature qui marche sans demande explicite.
2. Ne pas renommer / supprimer des workflows acquisition, tables `quiz_*` / `acq_*`, ou libellés produit sans GO.
3. Copy acquisition : jamais « Mangitas » ; pas « rendez-vous fixe » (→ cours collectifs à horaires fixes) ; prix = offre groupe 39€ d’abord.
4. Quiz PDF : logo PNG transparent ; mix couleurs page 2 ; titres collés aux bannières.

## Surfaces produit (carte mentale)

### Public
- Landing `/` + `/es` — essai 7j, Stripe Checkout
- Quiz hub `/quiz` + `/es/quiz` + `/quiz/[slug]` (profil-discipline = DiSC FitMangas)
- Blog, boutique Printful, pages SEO Pilates
- Live visio (`live.fitmangas.com` / Jitsi)

### Espace cliente (`/compte`, `/connexion`)
- Dashboard, replays, abonnement Stripe, profil, notifications

### Admin (`/admin`)
- `acquisition` — inbox DM, workflows, followups, Meta live
- `community` / `marketing` — CM Instagram (génération posts)
- `blog`, `clients`, `courses`, `replays`, `promos`, `inbox`, `support`, `boutique`, `videos`, `vimeo`, `attendance`, `croissance`, `notifications`

### Acquisition (code clé)
- Catalog workflows : `src/lib/acquisition/engine/workflow-catalog.ts` (sync DB via `ensureWorkflowCatalog`)
- Webhook Meta : `src/app/api/acquisition/webhooks/meta/route.ts` (anti-doublon mid + texte 2 min)
- Nurture quiz : `src/lib/quiz/lead-nurture.ts` (email immédiat + J+2/J+5 + WA)
- Cron : `/api/admin/acquisition/cron/run-followups`

### Quiz / Connaissance de soi
- Définition legacy DiSC : `src/lib/quiz/quizzes/profil-discipline*.ts` (nurture cron encore branché sur `quiz_leads`)
- **Moteur neuf** : `src/lib/self-knowledge/` — Big Five IPIP-50 **ou** IPIP-NEO-120 (choix cliente) + ECR-S ; banques déterministes traits/facettes/attachement ; **rapport = 100 % banque** (`SELF_TEST_CLAUDE_ANALYSIS` OFF) ; DA type profil-discipline (`SelfTestReport`, radar OCEAN labels hors points) ; hub `/quiz` = cartes verticales compactes (CTA « Je commence » above fold desktop) + portraits coach + stats ; fond questions `public/tests/questions-bg.jpg` ; carrousel vidéo sous le fold ; PDF Chromium full-bleed + marges contenu ; dédup email membre>acq>blog
- Pages `/quiz` ; compte `/compte/connaissance-de-soi` (hub 5 onglets DA immersive type `/quiz` : `HubVisuals` hero/empty/chaîne/jalons ; dashboard KPI → onglet Ma progression ; `/compte/progression` redirige ; affiliation `affiliate_url` + badge « Lien affilié » + `affiliate_clicks` ; doc `docs/AFFILIATION_DEMARCHE.md` ; historique 50/120 ; compare radar ; journal/défis)
- PDF self-test : print-to-PDF de la page rapport (`page.pdf` / `buildSelfTestReportPdfFromElement`) ; invites RGPD `self_test_invites` + `SelfTestShareInvite` ; priorité email member > acq > blog (`email-flow-priority.ts`)
- Archive code : `_archive/quiz-legacy-YYYY-MM-DD/`
- PDF legacy DiSC : `src/lib/quiz/build-report-pdf.ts`
- Lead gate legacy : `src/lib/quiz/lead.ts` → `quiz_leads` + `acq_contacts`

## Meta (re-vérifié 21/09/2026 — ne pas re-dire « expiré » sans test)
- Script : `npx tsx scripts/check-meta-tokens.ts`
- Page ID ≠ IG User ID OK ; tokens CM + IGAA valides
- WhatsApp robot branché ; templates marketing = 0
- **Bloquant WA réel** : WABA `1427605062481966` sans moyen de paiement → `health_status.can_send_message = BLOCKED` (code 141006). Pas de template / cold send tant que carte absente dans Billing Hub. Email + wa.me = filet OK.
- Template cible une fois paiement OK : `quiz_essai_fitmangas` (FR, MARKETING, corps **statique sans {{vars}}**)
- **Carte Meta** : Kevin débloque demain (carte bancaire temporairement bloquée) — reprendre alors paiement + template.

## CM / Programmation & publication
- Miroirs Reels IG : Facebook + TikTok + **YouTube Shorts** (`alsoPublishYouTube`, `src/lib/admin/youtube-social.ts`)
- YouTube = **publication seulement** (pas de DM → pas de workflows acquisition type IG/WA). OAuth : `YOUTUBE_CLIENT_ID` + `YOUTUBE_CLIENT_SECRET` — **connecté 21/09/2026** (`youtube_social_connection`, chaîne Alejandra Mangas `UCHkmseevG7-gcLcQZS5xVkQ`).
- Compte cible chaîne : `info@casamangas.com` (OAuth Google Cloud — pas le mot de passe dans le repo).
- **TikTok** (23/09/2026) : app Live `Fitmangas` ; Redirect URI OK `…/api/admin/community/tiktok/callback` ; OAuth scopes Live = `user.info.basic` + `video.upload` seulement (`video.publish` = révision TikTok à faire). Connexion compte `@fit.mangas` → `tiktok_social_connection` encore à finaliser (QR / session créateur).
- **WhatsApp** : WABA AVAILABLE (carte OK) ; téléphone `DISCONNECTED` + `ON_PREMISE` coexistence incomplète → templates bloqués **2494160** + envoi **133010**. Filet = email + wa.me. Support Meta requis pour pairing `CLOUD_API`.

## Git push
- Compte CLI `support811` = **pull only**
- Push via MCP GitHub authentifié **`fitmangas`** (PR/merge) ou PAT propriétaire
- Code acquisition/quiz 21/09 déjà sur `main` (`4f69330`)

## Contexte Cursor (anti « je réinvente »)
- **Dossier canonique** : `/Users/kevinpicard/Projets/fitmangas5` (≈120 transcripts). Ignorer la copie iCloud (vide).
- Toujours lire : source-de-verite + inventaire-fige + `docs/CONTEXTE_VIVANT.md` + `docs/archive-produit/`
- Fin de chantier validé → **l’agent** journalise dans CONTEXTE_VIVANT + archive (jamais demander à Kevin)
- Copie hors-repo : store perso Cursor `fitmangas-archive-produit`
