# Archive quiz legacy — 2026-09-21

Copie inerte de référence du système quiz FitMangas (DiSC / profil-discipline, etc.) avant remplacement par le moteur self-knowledge (IPIP-50 + attachement).

- **Ne pas importer** depuis l’app (hors routes, hors barrel).
- **Données** : table `quiz_leads` non touchée (archive code uniquement).
- Restauration : recopier vers `src/` si besoin, puis réintroduire routes.

Contenu :
- `src/lib/quiz/`
- `src/components/Quiz/`
- `src/app/quiz/` + `src/app/es/quiz/`
- `src/app/api/quiz/`
